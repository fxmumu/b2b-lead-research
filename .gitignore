# User-maintained configuration template (real leads.yaml lives in task working directories).
config/leads.yaml
# In case someone runs tasks inside a clone of this repo, ignore the per-task config too.
/leads.yaml

# Python bytecode
__pycache__/
*.pyc

# Runtime artifacts of research sessions
lead-research/
